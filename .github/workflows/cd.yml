name: Deploy Terraform Standalone

on:
  push:
    branches:
      - "**"

permissions:
  id-token: write
  contents: read

env:
  TF_WORKING_DIR: ./deploy

jobs:
  build-and-deploy:
    uses: Ascentt-Business-Systems/ascentt-mlops-github-actions/.github/workflows/cd-terraform-standalone.yml@feature/dev
    with:
      branch: ${{ github.ref_name }}
